import _ from 'lodash';
import css from '../styles/app.css';


function notify(msg) {
  $notice.html(msg);
}

var currentPlayer = 1,
  playername = '',
  players = [],
  $modalButton,
  $modalInput,
  $modalOverlay,
  $modalPrompt,
  $notice;

gameNotify
function notify(msg) {
  $notice.html(msg);
}

